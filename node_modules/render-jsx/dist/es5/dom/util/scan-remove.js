"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.scanRemove = void 0;
function scanRemove(start, end, options) {
    var opts = __assign({ includeStart: false, includeEnd: false, remove: function (n) { return n.parentElement.removeChild(n); }, callback: undefined }, options);
    var scanned = [];
    var _remscan = function (n, cb) {
        if (cb === void 0) { cb = function (_n) { return scanned.push(_n); }; }
        opts.remove(n);
        if (opts.callback) {
            opts.callback(n);
        }
        cb(n);
    };
    var cursor = start;
    while ((cursor = start.nextSibling) && cursor !== end) {
        _remscan(cursor);
    }
    if (opts.includeStart) {
        _remscan(start, function (n) { return scanned = [n].concat(scanned); });
    }
    if (opts.includeEnd) {
        _remscan(end);
    }
    return scanned;
}
exports.scanRemove = scanRemove;
//# sourceMappingURL=scan-remove.js.map