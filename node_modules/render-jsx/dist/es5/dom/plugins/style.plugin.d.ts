import { Plugin, PropPlugin, RendererLike } from '../../renderer';
export interface SetStylePlugin<R extends RendererLike<Node>> extends Plugin<Node, R> {
    setStyle(node: HTMLElement, style: string, target: any, set: (value: string | object) => void): boolean;
}
export declare class StylePlugin<R extends RendererLike<Node>> extends Plugin<Node, R> implements PropPlugin<Node> {
    private stylePlugins;
    plug(renderer: R): void;
    priority(): number;
    setProp(node: Node, prop: string, target: any): boolean;
    format(key: string, value: string): {
        key: string;
        val: string;
        unit: string;
    };
    setStyle(node: HTMLElement, style: string, value: string | object): void;
}
