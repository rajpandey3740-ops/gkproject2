import { Plugin } from './plugin';
import { RendererLike } from '../types';
export interface LeafPlugin<Node, Renderer extends RendererLike<Node> = RendererLike<Node>> extends Plugin<Node, Renderer> {
    leaf(): Node;
}
export declare function isLeafPlugin<Node, Renderer extends RendererLike<Node>>(plugin: Plugin<Node, Renderer>): plugin is LeafPlugin<Node, Renderer>;
