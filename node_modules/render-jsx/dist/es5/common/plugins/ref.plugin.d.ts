import { Plugin, PropPlugin } from '../../renderer';
export declare class RefPlugin<Node> extends Plugin<Node> implements PropPlugin<Node> {
    setProp(node: Node, prop: string, target: any): boolean;
    priority(): number;
}
