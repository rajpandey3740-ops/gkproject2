import { Plugin, PropPlugin } from '../../renderer';
export declare class ContentPropPlugin<Node> extends Plugin<Node> implements PropPlugin<Node> {
    setProp(node: Node, prop: string, target: any): boolean;
    priority(): number;
}
