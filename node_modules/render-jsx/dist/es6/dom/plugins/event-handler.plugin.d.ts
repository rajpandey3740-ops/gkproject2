import { Plugin, PropPlugin } from '../../renderer';
export declare class EventHandlerPlugin extends Plugin<Node> implements PropPlugin<Node> {
    priority(): number;
    setProp(node: Node, prop: string, target: any): boolean;
}
